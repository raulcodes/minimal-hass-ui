import { h } from 'preact';
import { useState } from 'preact/hooks';
import connect from '../../utils/websocketClient';

const Auth = () => {
	// connect();

	return(
		<div>
      <h2>Auth</h2>
			<input></input>
		</div>
	);
};

export default Auth;
